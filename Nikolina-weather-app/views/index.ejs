<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weatheer App</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div>
    <div class="container">
        <header>
            <div class="compas-icon"><img src="/icons/compass.svg" alt=""></div>
        </header>
        <div class="weather-title">
            <h1>Simply Weather</h1>
            <h4>View the current weather for your city</h4>
        </div>
        <form action="/" method="POST">
            <input type="text" name="city" class="ghost-imput" placeholder="Enter a city">
        </form>

        <div class="result">
            <% if(temp !== null)  {%>
                <h1 class="temp"><%= temp %></h1>
            <% } %>

            <div class="icons-des">
                <% if(icon !== null)  {%>
                    <img class="icon" src="https://openweathermap.org/img/wn/<%= icon %>.png" alt="missing weather-icon">
                <% } %>
                <% if(des !== null)  {%>
                    <h3><%= des %></h3>      
                <% } %>
                </div>
                <% if(city !== null)  {%>
                    <p class="city"><%= city %></p>
                    <% } %>
        </div>
    </div>
</body>
</html>